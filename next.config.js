/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["www.cnnbrasil.com.br"],
        formats: ["image/avif", "image/webp"],
    },
};

module.exports = nextConfig;
